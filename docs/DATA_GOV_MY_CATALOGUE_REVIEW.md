# data.gov.my Catalogue Review

The first data.gov.my sample-only probe succeeded for 3 datasets.

Reviewed report:

    reports/data_gov_my_catalogue_review.md

Machine-readable review:

    configs/data_gov_my_catalogue_review.json

Current decision:

- keep `population_district` as a supporting geography candidate
- defer `water_consumption` as weak context only
- defer `water_pollution_basin` as environmental context only
- do not treat any reviewed dataset as `flood_occurred`

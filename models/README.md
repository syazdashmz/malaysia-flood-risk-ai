# Model Artifacts

This folder stores local model artifacts.

Tracked files:

- `.gitkeep`
- `README.md`

Ignored local outputs:

- `kaggle_flood_baseline.joblib`
- `kaggle_flood_baseline_metadata.json`

Regenerate the experimental model with:

```powershell
.\scripts\run_experimental_ai_pipeline.ps1
```

The Kaggle model is experimental only. It is not the final official verified
Malaysia flood model.
